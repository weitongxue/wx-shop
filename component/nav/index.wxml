<!--component/nav/index.wxml-->
<view class="nav">
  <navigator class="nav-item" hover-class="none" wx:for="{{navList}}" wx:key="index" url="/pages/cart/cart">
    <image class="nav-icon" src="{{item.image}}" alt="{{item.title}}"></image>
    <text>{{item.title}}</text>
  </navigator>
</view>
